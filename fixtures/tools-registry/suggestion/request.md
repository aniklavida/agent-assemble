# Suggestion Request

> "We need an HTTP client, a cache, an optional search service and a metrics
> agent for the new service. What should we use?"

This is the shape of a real request: a slot to fill, not a demand for a
catalogue. The answer is the short list that survives the project's constraints
in [constraints.md](constraints.md), applied to [candidates.md](candidates.md).
